<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="14dp"
    android:orientation="vertical"
    android:padding="14dp"
    android:background="@drawable/bg_item_budget">

    <!-- ===== Row 1: Tên + % ===== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <FrameLayout
            android:id="@+id/layout_budget_icon"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:layout_marginEnd="12dp"
            android:background="@drawable/bg_circle_category">

            <ImageView
                android:id="@+id/img_budget_icon"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_category_tag"/>
        </FrameLayout>

        <TextView
            android:id="@+id/tv_budget_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Ăn uống"
            android:textColor="#202124"
            android:textStyle="bold"
            android:textSize="14sp"/>

        <TextView
            android:id="@+id/tv_budget_percent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="6dp"
            android:text="0%"
            android:textColor="#202124"
            android:textSize="14sp"
            android:textStyle="bold"/>

        <ImageView
            android:id="@+id/btn_edit_budget"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Sửa ngân sách"
            android:padding="7dp"
            android:src="@drawable/ic_edit"
            app:tint="@color/blueApp"/>

        <ImageView
            android:id="@+id/btn_delete_budget"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Xóa ngân sách"
            android:padding="7dp"
            android:src="@drawable/ic_delete"
            app:tint="#D32F2F"/>
    </LinearLayout>

    <!-- ===== Row 2: Tiền ===== -->
    <TextView
        android:id="@+id/tv_budget_amount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0đ / 0đ"
        android:textSize="13sp"
        android:layout_marginTop="4dp"
        android:textColor="#666"/>

    <!-- ===== Progress ===== -->
    <ProgressBar
        android:id="@+id/progress_budget"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:layout_marginTop="12dp"
        android:progress="0"
        android:max="100"
        android:progressDrawable="@drawable/bg_progress_budget"/>

    <TextView
        android:id="@+id/tv_budget_warning"
        android:layout_width="match_parent"
        android:layout_height="26dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/bg_budget_warning_soft"
        android:gravity="center_vertical"
        android:paddingHorizontal="10dp"
        android:textColor="#F57C00"
        android:textSize="11sp"
        android:textStyle="bold"
        android:visibility="gone"
        tools:text="SẮP CHẠM HẠN MỨC"
        tools:visibility="visible"/>

</LinearLayout>
